# DELVE_MC_maps
Website to look through DELVE-MC combined images.

## Usage

You need to provide your own combined images, which need to be in their own folder called "images" inside the "data" folder. Each image needs to have the filename be: BRICKNAME\_comb.png
